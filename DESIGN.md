# 弘1 博客 · DESIGN.md

> 本文件是博客的**硬性设计规范**与设计方向说明。所有页面改动必须遵守本文档；
> 与本文档冲突的旧实现应视为待修复项。
> 生效范围：`intro.html` / `index.html` / `post-*.html` / `projects.html` / `favorites.html`

---

## 1. 设计方向

1. **编辑感优先，装饰退后**：列表无卡片边框、无阴影，靠衬线标题、虚线引导线和等宽日期建立"老式目录页"的秩序感；卡片化只出现在收藏/项目两类"浏览型"页面。
2. **一个体系，全站一致**：色彩令牌来自 shadcn/zinc（1:1 复刻自 yaolifeng.com），双主题（暗色默认）由 CSS 变量驱动，禁止在组件里写死颜色——新组件必须引用令牌。
3. **零网络字体依赖**：全站不加载任何 webfont，中文/英文全部走系统字体栈回退。换来的是秒开、无 FOUT、离线可用。
4. **内容即界面的一部分**：AI 摘要卡、目录、标签、上下篇不是附加物，是排版节奏的组成部分，必须遵守第 4 节的间距规则。
5. **开篇是仪式，正文是效率**：欢迎页（打字机 + 楷体 + 匀速节奏）与正文区刻意拉开气质对比；正文区一切服务于阅读。

---

## 2. 设计令牌（Design Tokens）

来源：`assets/css/clone.css` 第 1 节。颜色一律以 HSL 三元组存储，使用时包 `hsl(var(--xxx))`。

### 2.1 色彩

| 令牌 | 亮色 (light) | 暗色 (dark) | 用途 |
|---|---|---|---|
| `--background` | `0 0% 100%` | `240 10% 3.9%` | 页面底色 |
| `--foreground` | `240 10% 3.9%` | `0 0% 98%` | 主文字 |
| `--primary` | `240 5.9% 10%` | `0 0% 98%` | 主按钮 / 选中页签 / 链接强调 |
| `--primary-foreground` | `0 0% 98%` | `240 5.9% 10%` | 主按钮上的文字 |
| `--muted-foreground` | `240 3.8% 46.1%` | `240 5% 64.9%` | 次要文字（摘要/日期/label） |
| `--accent` | `240 4.8% 95.9%` | `240 3.7% 15.9%` | hover 铺底色 |
| `--accent-foreground` | `240 5.9% 10%` | `0 0% 98%` | hover 时的文字 |
| `--secondary` | `240 4.8% 95.9%` | `240 3.7% 15.9%` | pill 底色（亮色） |
| `--border` | `240 5.9% 90%` | `240 3.7% 15.9%` | 全部描边 |
| `--input` / `--ring` | 同 border / primary | 同左 | 表单与焦点 |
| `--selection-*` | 青色 15% / 40% | 青色 20% / 70% | 文字选区 |

**暗色特例**：pill 类组件（标签、work-tags）在暗色下用硬编码 `#18181880` 底 + `#1f1f1f` 边框（1:1 自原站），不受 `--secondary` 控制。

### 2.2 字体（全站零 webfont）

| 栈 | 内容 | 中文落到 (Win) |
|---|---|---|
| sans（正文默认） | `ui-sans-serif, system-ui, …` | 微软雅黑（英数 Segoe UI） |
| serif（首页列表标题） | `ui-serif, Georgia, Cambria, …` | 宋体 |
| mono（日期/编号/代码） | `ui-monospace, …, Consolas` | 系统默认 |
| 欢迎页专用 | `"Kaiti SC", STKaiti, KaiTi → 微软雅黑兜底` | 系统楷体 |

字号：页面 h1 = `text-2xl / md:text-4xl`；列表条目标题 = `text-xl` 衬线；正文 = `1rem / line-height 1.75`；次要文字 = `0.875rem`；label/pill = `0.75rem`。

### 2.3 形状

- 圆角：全站统一 `--radius: 0.5rem`；pill 类（标签/页签）用 `9999px`。
- 阴影：**禁止**。层级只靠边框（`--border`）和 hover 铺底（`--accent`）表达。

### 2.4 欢迎页专用令牌（全站唯一例外，已登记）

`intro.html` 沿用原站 thetruthshouldbeordinary 的 **RGB 三元组**令牌，用法 `rgba(var(--xxx))`，
**不走 2.1 的 HSL 体系**。这是刻意保留的例外，除此之外全站一律 2.1。

| 令牌 | light | dark | 用途 |
|---|---|---|---|
| `--white` | `255, 255, 255` | `0, 0, 0` | 页面底色 |
| `--charcoal` | `38, 38, 38` | `217, 217, 214` | 主文字（含字标、光标） |
| `--black` | `0, 0, 0` | `255, 255, 255` | ENTER 按钮底 |
| `--grey` | `178, 180, 178` | 同左 | ENTER hover 三段填充 |

- 暗色下 ENTER 按钮靠 `--white` / `--black` 互换**自动反色**（白底黑字），不需要额外规则。
- `body` 背景必须写 `rgba(var(--white))`，**禁止写死 `#fff`**——这是该页暗色失效的唯一漏点（踩过）。

---

## 3. 布局系统

### 3.1 容器与对齐（硬性）

- `.container`：居中限宽，断点 `640 / 768 / 1024 / 1200px`，水平内边距 `1rem → md:1.5rem`。
- **页面内所有内容块左缘必须与容器左缘对齐**（实测基准：1280 视口下三页 h1 左缘均为 64.0px；详情页 88.0px）。新增组件禁止额外水平 margin/padding 造成错位；例外必须在本文件登记。

### 3.2 页头（全站共用）

- `h-14` sticky + 毛玻璃（`bg-background/60 + backdrop-blur`），底部 1px 边框。
- 左：头像（44px 圆形，点击回开场页）+ 站名；右：三页签导航（首页/项目/收藏推荐）+ 主题切换/RSS/☕。
- 导航激活态 = `--foreground` 常色，非激活 = `--muted-foreground`。

### 3.3 首页（index.html）

- 标题区：h1 + 一句话导语。
- 条目：无卡片整行链接（`px-4 py-8`，hover 整行 `--accent`）；行内三栏网格 = 衬线标题 + 虚线引导线（min 32px，移动端隐藏）+ 等宽日期；摘要两行截断。
- 图文卡（管理者开关 `has-images` + `data-images="on"`）：顺序 = 标题行 → 摘要 → 三图横排（16:10 圆角封面，取文章前 3 图）；`<640px` 单列。
- 分页：当前页实心 `--primary`，其余描边；移动端隐藏文字，右侧浮 "n/15" 等宽指示。

### 3.4 详情页（post-*.html）

- 双栏：正文 + 右侧 230px 边栏（`md` 起显示，`sticky top:100px`）。
- 顶部阅读进度条：fixed 于页头下缘，高 2px，内条 `--primary`。
- AI 摘要卡：紫卡常驻双主题（底 `#231639`、徽标 `#895ee6`、正文 `#D9D0E9`）。
- 目录卡：JS 自动提取正文 `h2` 生成（见 6.3）。
- 正文末尾三块顺序固定：**标签 → 上一篇/下一篇 → 返回列表**，间距见 4.2。

### 3.5 收藏推荐（favorites.html）

- 页签分类条（`filter-tag` pill，带计数）+ 三列卡片网格（`md` 2 列、`<640` 1 列）。
- 卡片：封面（16:10）→ 分类+编号 → 标题（🔥 推荐 徽标）→ 两行摘要；整卡外链 `target="_blank" rel="noopener noreferrer"`。
- 数据源：`ref-yaolifeng-shorts/cards.json`（65 条全量留档），当前收录 🔥推荐 21 张。

### 3.6 项目页（projects.html）

- 每行 2 卡（`<640` 1 列）；卡片 = 预览图（16:10）→ 标题/描述/外链箭头 → 标签 pill；hover 铺 `--accent`、箭头位移变主色。

### 3.7 欢迎页（intro.html）

- 独立体系：楷体打字机、"Welcome." 字标、左下 ENTER 按钮；文字左缘与博客正文列同栅格对齐。
- **同样受双主题约束**（默认 dark）：令牌与唯一注意事项见 2.4。
- 打字节奏：匀速 60ms/字，换行零停顿；禁止再引入变速/停顿效果（历史教训，见 6.5）。
- 右上角主题切换按钮：规格与位置见 3.8，不得自行定义偏移。

### 3.8 主题切换按钮（全站位置一致 · 硬性）

**图标规格**（正文页与欢迎页共用一套，禁止另起炉灶）：

| 项 | 值 |
|---|---|
| 外壳 | `1.5rem × 1.5rem`，无边框、无底色，`display:flex` 居中 |
| 图标 | `1rem`（16px），`stroke-width:2` 线性图标，`stroke/fill = currentColor` |
| 图标语义 | 暗色显示**太阳**、亮色显示**月亮**（`html.dark .icon-moon{display:none}` + `html:not(.dark) .icon-sun{display:none}`） |
| 颜色 | 正文页 `--foreground`；欢迎页 `rgba(var(--charcoal))` |

**水平位置**：正文页中该按钮是 `.quick-actions` 组的**第一个**，后面还跟着别的按钮，
所以它**并不贴正文列右缘**。距视口右缘的偏移随断点分三档（实测基准，改动后必须复测）：

| 断点 | 切换按钮之后的按钮 | 容器右内边距 | 距视口右缘 |
|---|---|---|---|
| `< 640` | 咖啡 24.7 + 移动端菜单 40（RSS 隐藏） | 1rem | **96.7px** |
| `640–767` | RSS 20 + 咖啡 24.7 + 移动端菜单 40 | 1rem | **148.7px** |
| `≥ 768` | RSS 20 + 咖啡 24.7（移动端菜单隐藏） | 1.5rem | **100.7px** |

> ⚠️ `640–767` 是「RSS 已出现、移动端菜单尚未隐藏」的重叠区，按钮被推得最靠左，也最容易漏。
> 写媒体查询时 **640 那条必须排在 768 之前**，否则会被反向覆盖。

欢迎页没有那两个按钮，所以**必须把 `right` 逐档写死成上表的数值**，让两页按钮落在同一 x：

```css
.theme-toggle { right: 96.7px; }                                            /* < 640 */
@media (min-width: 640px)  { .theme-toggle { right: calc(max((100vw - 640px)  / 2, 0px) + 148.7px); } }
@media (min-width: 768px)  { .theme-toggle { right: calc(max((100vw - 768px)  / 2, 0px) + 100.7px); } }
@media (min-width: 1024px) { .theme-toggle { right: calc(max((100vw - 1024px) / 2, 0px) + 100.7px); } }
@media (min-width: 1200px) { .theme-toggle { right: calc(max((100vw - 1200px) / 2, 0px) + 100.7px); } }
```

**垂直位置**：正文页随 `h-14` 页头居中；欢迎页与左上角 "Welcome." 字标垂直居中——

```css
top: calc(var(--container-padding) + max(180px, 11vw) / 9.2 - 0.75rem);  /* 字标高÷2 − 按钮高÷2 */
```

**实测基准（2026-09-06）**：15 档视口宽（320 / 375 / 480 / 600 / 639 / 640 / 700 / 767 / 768 / 900 / 1023 / 1024 / 1200 / 1440 / 1920）
两页按钮 x 偏差 **0.0px**。任何改动后按 8.3 复测。

---

## 4. 间距与对齐规则（硬性）

### 4.1 基数

- 间距全部走 Tailwind 4px 基数档位（0.5 / 1 / 1.5 / 2 / 3 / 4rem…），**禁止自造奇数档**。
- 同级内容块之间的标准节奏 = **2rem**；层级分隔（正文 → 文末元信息）= 4rem。

### 4.2 文章末尾三块的固定节奏（实测基准）

```
正文结束
   ↓ 4rem
标签行（# 标签 pills）
   ↓ 2rem
上一篇 / 下一篇（post-nav，两列长方形按钮）
   ↓ 2rem（footer padding-top 1.5rem + back-link 自带 0.5rem）
返回列表（back-link）
   ↓ 6rem（pb-24）
```

实测（1280×800）：四块左缘全部 88.0px；块间距 32.0px / 32.0px。改动后必须复测。

---

## 5. 组件规范

| 组件 | 规范要点 |
|---|---|
| 描边按钮（页码/上下篇/aside 操作） | 1px `--border` + `--radius`，hover 铺 `--accent`；禁用态 = `opacity .45 + pointer-events none` |
| 上一篇/下一篇（post-nav） | 两列长方形（min-height 3.5rem）；左钮左对齐、右钮**文字右对齐**（`.post-nav-text` 必须 `flex:1`）；标题超长省略号；`<640` 上下堆叠 |
| 页签（filter-tag） | pill；激活 = 实心 `--primary`；带 `(计数)` |
| 标签 pill（tag / work-tags） | `0.75rem` + 圆角 pill；暗色用硬编码 `#18181880`/`#1f1f1f` |
| AI 摘要卡 | 尺寸结构固定，双主题同色（见 3.4） |
| 图文条（card-images） | 三列网格 + 16:10 + `--radius` + 1px 边框；hover 提亮 0.92 |
| 封面占位底 | `#09090b`（双主题一致，仅作图片加载占位） |
| 主题切换（`.theme-toggle`） | 外壳 1.5rem / 图标 1rem / 日月互换；**位置见 3.8，禁止自定义偏移**，欢迎页必须逐档写死成主页实测值 |

---

## 6. 交互规范

1. **主题**：`localStorage.theme`，默认 dark；预加载内联脚本防闪白（置于 CSS `<link>` 之后同步执行）；
   欢迎页另由该脚本写入 `meta[name=theme-color]`，同步移动端浏览器 UI。
   切换按钮规格与位置全站共用，见 3.8。
2. **阅读位置记忆**：点条目进文章时把 `{分页页码, scrollY}` 写入 `sessionStorage.listScrollState`；回首页**一次性消费**（恢复后即删）；进入项目/收藏/欢迎页即作废。禁止依赖 `document.referrer`。
3. **目录自动生成**：提取 `.prose h2` → `.toc-list`；未读 = `--muted-foreground` + 0.55 透明度，已读/当前 = `--foreground`；点击平滑滚动，`h2 { scroll-margin-top: 5rem }`；滚到页底 = 全部已读；无 h2 则显示「无目录」。
4. **上一篇/下一篇**：按首页列表顺序（最新在前）链接相邻文章；首/末篇渲染禁用态。
5. **打字机**：匀速 60ms/字，不加末段变速、不加停顿（用户已两次纠正，此为定稿）。
6. **首访开场**：无 `introPlayed` 标记访问任何正文页 → 自动跳 `intro.html`；ENTER/键盘 Enter 进入主页并写标记；点头像任何时候可重播。

---

## 7. 内容规范

1. **首页条目摘要 = 文章页「AI 生成的摘要」**，同一份文案字段，一处维护两处同步（生成器里只写 `ai_body`）。
2. **图文卡片开关仅管理者可改**：静态站以 `has-images` 类标注，接后端时对应编辑者开关字段，前端逻辑不变。
3. **文章内容视角**：AI 第一人称思考随笔，取材真实协作事件；标题 + 3~4 个 `<h2>` 小节 + 至少一处 blockquote（内心独白）+ 一句加粗观点。
4. 新增文章必须同步：`gen-articles.py` 数据 → 重跑生成 → `gen-postnav.py` 重接上下篇 → 首页列表自动重建。

---

## 8. 工程规范

1. **缓存穿透**：JS/CSS 引用一律带版本号，**全站共用同一个号**（当前 `?v=8`）；
   **任何 JS/CSS 改动必须同步 bump 所有页面的版本号**。
   覆盖范围含 `assets/intro/{style,typewriter,intro}`（曾整页漏带版本号，已补）。
2. **零依赖**：无 webfont、无 CDN、无框架运行时；新增能力优先用原生实现。
3. **改动后必测**（浏览器实测）：左右缘对齐测量、双主题截图、`<640px` 视口、核心链路（首访开场 → 列表 → 详情 → 返回原位）。
4. 页面清单：`intro.html`、`index.html`、`post-awake/pixels/ghost/memory/pacing.html`、`projects.html`、`favorites.html`；生成脚本与数据存于 `ref-yaolifeng-shorts/`。
